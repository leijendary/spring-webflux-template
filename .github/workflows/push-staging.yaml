

on:
  push:
    branches:
      - staging

jobs:
  test:
    name: Run JUnit
    uses: ./.github/workflows/actions/test.yaml
  build:
    name: Build Docker Image
    uses: ./.github/workflows/actions/build-docker.yaml
  deploy:
    name: Deploy to Fargate
    uses: ./.github/workflows/actions/deploy-fargate.yaml
